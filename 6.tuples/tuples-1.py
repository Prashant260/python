# Tuple is a data type in which we can store the different types of data type
# how to define the tuple 
T=()
print(type(T))

#we can store any type of datatype in the tuple
T1=(90,50,40,'suhani', 's', 49.2)
print(T)
print(T1[2])
print(T1[0:5:1])


#differece between list and tuple 
print(type(T1))
print(T1)
S1=["suhani", "aanchal" , "ram" , 's',5,6]

print(type(S1))
print(S1)

#inserting some item in the tuple
T1.append((0), "pqrs")
#so tupples are fixed when they are defined there is no way to add new items into it .

